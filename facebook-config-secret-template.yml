apiVersion: v1
kind: Secret
metadata:
  name: facebook-config
type: Opaque
data:
  # Put here your facebook app credentials (base64 encoded `echo -n "originalvalue" | base64`), then `oc create -f this-file.yml`
  fb-page: AAAA
  fb-app-id: BBBB
  fb-app-secret: CCCC
  fb-access-token: DDDD

